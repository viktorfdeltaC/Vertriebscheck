VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Server-only — set via `supabase secrets set SUPABASE_SERVICE_ROLE_KEY=…`
# Never expose to client code; consumed only by the invite-user Edge Function.
# SUPABASE_SERVICE_ROLE_KEY=…
